{
  "name": "my-poc",
  "version": "1.0.0",
  "description": "Node.js static code analysis POC with tests and code coverage",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=lcov --reporter=text mocha",
    "coverage": "nyc report --reporter=lcov > coverage/lcov.info",
    "qlty": "bash .qlty/qlty.sh"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lodash": "4.17.4"   // 🔴 vulnerable version for OSV scanner to catch
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  }
}
