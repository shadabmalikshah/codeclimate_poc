{
  "name": "my-poc",
  "version": "1.0.0",
  "description": "Node.js static code analysis POC with tests and code coverage",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=lcov --reporter=text --check-coverage --lines 80 --branches 80 --functions 80 --statements 80 mocha",
    "coverage": "nyc report --reporter=lcov > coverage/lcov.info",
    "qlty": "bash .qlty/qlty.sh"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  }
}
